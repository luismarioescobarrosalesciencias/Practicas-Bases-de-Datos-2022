package SRC;
import java.text.ParseException;
import java.util.LinkedList;
public class Main {
    /**
     *  main Metodo del programa a ejecutar desde terminal, utiliza recursivamente el menu
     * @param args
     */
    public static void main(String[] args) {
        Menu menu = new Menu();
        menu.menu();
    }
}
